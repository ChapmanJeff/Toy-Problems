var isPalindrome (str) {
	var string = str.split(' ').join('')reverse().join('').toLowerCase();
	str = str.split(' ').join('').toLowerCase();
	if(string === str) {
		console.log(true)
	}
	console.log(false)
}

